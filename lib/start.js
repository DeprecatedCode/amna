/**
 * AMNA: Amazing Mongoose Node.js API
 *
 * @author Nate Ferrero
 * @url https://github.com/NateFerrero/amna
 *
 * AMNA - Start
 */
module.exports = function (amna, log) {
	return function (config) {
		log(config);
	};
	//'mongodb://gobbl-prod:​this-is-the-gobbl-prod-password@' + mongo.join(',') + '/gobbl'
    //;
};
